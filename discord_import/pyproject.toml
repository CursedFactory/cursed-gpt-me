[project]
name = "discord_import"
version = "0.1.0"
description = "Discord message import and processing"
requires-python = ">=3.8"
dependencies = [
    "discord.py>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"